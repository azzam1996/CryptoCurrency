package com.azzam.cryptocurrency.currencies.domain.utils

enum class NetworkError {
    REQUEST_TIMEOUT,
    DAD_REQUEST,
    UNAUTHORIZED,
    NOT_FOUND,
    NO_INTERNET,
    SERVER_ERROR,
    JSON_PARSE_ERROR,
    UNKNOWN,
}